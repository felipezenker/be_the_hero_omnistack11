/**
 * Rota / Recurso
 */

/**
 * Métodos HTTP:
 * 
 * GET:    Buscar/listar uma informação do back-end
 * POST:   Criar uma informação no back-end
 * PUT:    Alterar uma informação no back-end
 * DELETE: Deletar uma informação no back-end
 */

 /**
  * Tipos de parâmetros:
  * 
  * Query Params: Parâmetros nomeados enviados na rota após "?" (Filtros, paginação)
  * Route Params: Parâmetros utilizados para identificar recusros
  * Request Body: Corpo da requisição, utilizado para criar ou alterar recursos
  * 
  * //const params = request.query; // request.query / request.params / request.body
  */

  /**
   * SQL:   MySQL, SQLite, PostgreSQL, Oracle, MSSQL
   * NoSQL: MongoDB, CouchDB, etc
   */

   /**
    * Driver:        SELECT * FROM users
    * Query Builder: table('users').select('*').where()
    */
   

 /**
 * Entidades:
 *  - ONG
 *  - Casos (incident)
 * 
 * Funcionalidades:
 *  - Login de ONG
 *  - Logout de ONG
 *  - Cadastro de ONG
 *  - Cadastrar novos casos
 *  - Deletar casos
 *  - Listar casos especificos de uma ONG
 *  - Listar todos os casos (Mobile)
 *  - Entrar em contato com a ONG: Whats, e-mail (Mobile)
 */